apiVersion: v1
kind: Service
metadata:
  name: nginx-rbac-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: nginx-rbac
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
