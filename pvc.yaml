apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-shadow-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Mi
  storageClassName: local-path
